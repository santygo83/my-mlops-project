replicaCount: 2

image:
  repository: ghcr.io/santygo83/mlopsimg
  tag: latest
  pullPolicy: Always

container:
  port: 8000

service:
  type: NodePort
  port: 80
  targetPort: 8000

labels:
  app: diabetes-api

